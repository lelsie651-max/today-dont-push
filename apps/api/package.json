{
  "name": "@today-dont-push/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify 后端服务（本轮仅 GET /health）",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@today-dont-push/contracts": "workspace:*",
    "fastify": "^5.5.0"
  },
  "devDependencies": {
    "@today-dont-push/config": "workspace:*",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
